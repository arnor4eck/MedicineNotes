services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=medicinenotes'
      - 'POSTGRES_PASSWORD=secret'
      - 'POSTGRES_USER=admin'
    ports:
      - '5433:5432'
